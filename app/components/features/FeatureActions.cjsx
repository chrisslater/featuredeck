React = require('react')

Component = React.createClass

  getDefaultProps: (props) ->

  render: () ->

    (
      <div>
        <a>Up</a> <a>Down</a>
      </div>
    )

module.exports = Component
